This is not a fully working solution. As there is limited time.

TODOs:

1. Fully working tokenizer, it should not omit punctuations and the TextFilter should be able to reconstruct the original message with punctuation. I may need to modify the filters as well to ignore punctuation (this depends on the approach).
3. Tests for WordSpec, TextFilter and maybe Tokenizers. I prioritized unit tests for the filters. Maybe could have done more test cases for filters also.
